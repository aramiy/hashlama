name: ci

on:
  push:
    branches: main

jobs:
  login:
    runs-on: ubuntu-latest
    steps:
      -
        name: Login to harbor Container Registry
        uses: docker/login-action@v2
        with:
          registry: 108.143.193.224
          username: ${{ github.HARBOR_USER }}
          password: ${{ github.HARBOR_PASS }}
