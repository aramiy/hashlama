name: build and deploy 

on:
  push:
    branches: ["main"]
  
jobs:
  build: 
    name: Build 
    runs-on: ubuntu-latest
    steps:
      -name: Login to Registry
       uses: docker/login-action@v2
       with:
          username: ${{ secret.HARBOR_USER }}
          password: ${{ secret.HARBOR.PASS }}
          
    
  
    
